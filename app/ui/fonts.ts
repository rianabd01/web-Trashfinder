import { Inter, Lusitana, Quicksand } from "next/font/google";

export const inter = Inter({ subsets: ["latin"] });
export const quicksand = Quicksand({ subsets: ["latin"] });

export const lusitana = Lusitana({
  subsets: ["latin"],
  weight: "400",
});
